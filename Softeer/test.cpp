#include<iostream>
#include<math.h>
#include<queue>

using namespace std;
int main(int argc, char** argv)
{
	int n, t, w, k;
	int a[20]={};
	scanf("%s", &a);
	for(int i=0; i<20; i++)
	{
		if(char('A')<=a[i]&&a[i]<=char('Z')) printf("%c", a[i]);
	}
	queue<int> A, B, C, D;
	


	printf("\n%d", (int)(pow(2,31)-1));
	return 0;
}